# Command-Field-Parser
Lightweight C++ framework for command field parsing and validation. Supports universal and specific commands (e.g. compile, run), with format checks, missing/redundant field detection, and extensible FieldSpec-based validation for flexible multi-command protocols.
